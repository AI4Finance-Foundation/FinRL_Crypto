# 🚀 FinRL Crypto - Настройка для Binance

Этот репозиторий готов к работе с Binance API для криптовалютной торговли с использованием Deep Reinforcement Learning.

## 📋 Что уже сделано

✅ **Обновлены зависимости** - все пакеты совместимы с Python 3.8+
✅ **Исправлены устаревшие методы** - код совместим с новыми версиями pandas и numpy
✅ **Добавлена обработка ошибок** - улучшена стабильность работы
✅ **Оптимизирована производительность** - заменены устаревшие методы pandas

## 🔧 Требования

- **Python 3.10** (рекомендуемая версия)
- API ключи от Binance

## ⚙️ Настройка

### 1. Установка зависимостей

**Для Python 3.10 (рекомендуется):**
```bash
pip install -r requirements-python310.txt
```

**Для Python 3.11+:**
```bash
pip install -r requirements.txt
```

### 2. Настройка API ключей Binance

1. Зайдите в свой аккаунт Binance → API Management
2. Создайте новые API ключи
3. Откройте файл `config_api.py` и замените заглушки:

```python
API_KEY_BINANCE = 'ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ'
API_SECRET_BINANCE = 'ВАШ_СЕКРЕТНЫЙ_КЛЮЧ'
```

### 3. Проверка конфигурации

Основные параметры настроены в `config_main.py`:

- **TIMEFRAME**: '5m' - 5-минутные свечи
- **TICKER_LIST**: Список криптовалют для торговли
- **TECHNICAL_INDICATORS**: Технические индикаторы (MACD, RSI, CCI, DX)

## 🎯 Быстрый старт

### 1. Загрузка данных

```bash
python 0_dl_trainval_data.py  # Загрузка данных для обучения
python 0_dl_trade_data.py     # Загрузка данных для торговли
```

### 2. Оптимизация параметров

```bash
python 1_optimize_cpcv.py    # Combinatorial Purged Cross-Validation
# или
python 1_optimize_kcv.py      # K-Fold Cross-Validation
# или
python 1_optimize_wf.py       # Walk-Forward optimization
```

### 3. Валидация

```bash
python 2_validate.py
```

### 4. Бэктестинг

```bash
python 4_backtest.py
```

## 🔍 Основные компоненты

### 📁 Файлы проекта

- **`config_main.py`** - Основная конфигурация
- **`config_api.py`** - API ключи
- **`processor_Binance.py`** - Обработка данных Binance
- **`environment_Alpaca.py`** - Торговая среда
- **`drl_agents/`** - DRL алгоритмы (PPO, A2C, DDPG, TD3, SAC)

### 🎯 Поддерживаемые криптовалюты

- AAVE, AVAX, BTC, ETH, LINK, LTC, MATIC, NEAR, SOL, UNI
- Можно легко добавить новые в `config_main.py`

### 📊 Технические индикаторы

- MACD (Moving Average Convergence Divergence)
- RSI (Relative Strength Index)
- CCI (Commodity Channel Index)
- DX (Directional Movement Index)

## ⚠️ Важные замечания

1. **API ключи**: Никогда не храните API ключи в публичных репозиториях
2. **Тестирование**: Сначала протестируйте на демо-аккаунте
3. **Риски**: Торговля криптовалютами сопряжена с высокими рисками
4. **Версии**: Убедитесь, что используете Python 3.8+

## 🔧 Решение проблем

### Ошибка "API keys not configured"
- Проверьте файл `config_api.py`
- Убедитесь, что ключи введены правильно

### Ошибка "No data returned"
- Проверьте интернет-соединение
- Убедитесь, что символы криптовалют корректны
- Проверьте ограничения API Binance

### Проблемы с зависимостями
```bash
# Установка TA-Lib (может потребоваться)
conda install -c conda-forge ta-lib
# или
brew install ta-lib  # macOS
```

## 📈 Дополнительные возможности

### Визуализация результатов
Результаты сохраняются в папке `/plots_and_metrics/`

### Настройка параметров
- Измените даты в `config_main.py`
- Настройте технические индикаторы
- Модифицируйте списки криптовалют

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи ошибок
2. Убедитесь, что все зависимости установлены
3. Проверьте права доступа API ключей

---

**Готов к использованию!** 🎉

Проект полностью настроен для торговли на Binance с использованием передовых алгоритмов Deep Reinforcement Learning.